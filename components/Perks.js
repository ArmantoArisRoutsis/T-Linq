import React from 'react';
import ReactDOM from 'react-dom';

export default function Perks({ children }) {


    const smallScreen = () => {

    }
    return (
            <div className="newitem" style={{margin: "0 auto",
                position: "absolute",
                top: "40%"
            }}>{children}</div>
        
    )
}